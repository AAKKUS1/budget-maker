const createButton = document.getElementById('create-item');

createButton.addEventListener('click', () => {
    const menuOverlayer = document.createElement('div');
    const menu = document.createElement('section');

})